import { Colors } from './Colors';
// import { Fonts } from './Fonts';
import {Theme as RNETheme} from 'react-native-elements';

export const Theme: RNETheme = {
  Button: {
    buttonStyle: {
      backgroundColor: Colors.primary,

    },
    titleStyle: {
      color: "#000",
    }
  },
};
